generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model Recipe {
    id          String @id @default(uuid())
    nick        String @unique
    name        String
    description String
    text        String
}
